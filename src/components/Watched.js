import SearchBar from "./SearchBar";
//TODO WAtched, if stat.watched and unwatched , num ,error message

const Watched = () => {
  return (
    <div>
      <SearchBar></SearchBar>
    </div>
  );
};

export default Watched;
